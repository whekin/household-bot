server {
  listen 8080;
  server_name _;

  root /usr/share/nginx/html;
  index index.html;

  location = /health {
    default_type application/json;
    return 200 '{"ok":true}';
  }

  location / {
    try_files $uri $uri/ /index.html;
  }
}
